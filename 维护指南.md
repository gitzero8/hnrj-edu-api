# 仓库维护指南

本文档说明 hnrj-edu-api 仓库的日常维护方式，包括提交、推送、敏感文件纪律与常见问题处理。

## 目录

1. [日常提交工作流](#1-日常提交工作流)
2. [本机环境问题处理](#2-本机环境问题处理)
3. [敏感文件纪律](#3-敏感文件纪律)
4. [原始文档与脱敏副本同步](#4-原始文档与脱敏副本同步)
5. [提交前自查](#5-提交前自查)
6. [分支策略](#6-分支策略)
7. [换电脑 / 重新获取代码](#7-换电脑--重新获取代码)
8. [出错回滚](#8-出错回滚)
9. [本地备份提醒](#9-本地备份提醒)

## 1. 日常提交工作流

```powershell
cd f:\hnrj-edu-api
git add -A
git commit -m "描述你改了什么"
git -c http.sslBackend=schannel push
```

说明：

- commit 信息建议使用约定式提交风格，例如 `feat: 新增xxx接口`、`fix: 修复xxx问题`、`docs: 更新文档`
- 若已完成第 2 节的仓库级配置，最后一步可直接 `git push`

### 1.1 只提交或更新某个文件

当只想提交某一个文件时，`git add` 后面指定文件路径，而不是 `git add -A`：

```powershell
git add "维护指南.md"                  # 只暂存这个文件
git commit -m "docs: 更新维护指南"      # 提交只包含这个文件
git push
```

一次提交多个指定文件：

```powershell
git add edu-api-server/app.py edu-api-server/database.py
git commit -m "fix: 修复登录与课表接口"
git push
```

未指定的文件改动会留在工作区，不会被本次提交包含。

常用辅助命令：

| 想做什么 | 命令 |
|---------|------|
| 看当前哪些文件有改动 | `git status` |
| 只看某文件的改动内容 | `git diff <文件>` |
| 看已暂存了什么 | `git diff --cached` |
| 撤销某文件的暂存 | `git restore --staged <文件>` |
| 丢弃某文件的未提交改动 | `git restore <文件>`（慎重，会丢失） |
| 只提交某文件的一部分改动 | `git add -p <文件>`（逐块交互选择） |
| 看某文件的提交历史 | `git log --oneline -- <文件>` |

## 2. 本机环境问题处理

### 2.1 SSL 证书问题

本机 Git 默认使用 OpenSSL 后端且缺少 CA 证书，直接推送会报错：

```
SSL certificate problem: unable to get local issuer certificate
```

**一次性修复（推荐）**：在仓库目录执行，之后可直接 `git push`：

```powershell
cd f:\hnrj-edu-api
git config http.sslBackend schannel
```

**临时方案**：每次推送时带上参数：

```powershell
git -c http.sslBackend=schannel push
```

### 2.2 推送凭据

- 推送授权由 Git Credential Manager 自动管理，首次推送会弹出浏览器窗口完成 GitHub OAuth 登录
- 若推送报 `403 Permission denied`，通常是缓存的旧 PAT 失效，清除后重新推送即可：

```powershell
"protocol=https`nhost=github.com`n`n" | git credential-manager erase
git push    # 会重新弹出浏览器授权窗口
```

## 3. 敏感文件纪律（最重要）

仓库通过根目录 `.gitignore` 排除了以下内容，**不要使用 `git add -f` 强制添加**：

| 文件 / 目录 | 原因 |
|-------------|------|
| `edu-api-server/edu_system.db` | 数据库文件，启动服务时由 seed 数据自动重建 |
| `edu-api-server/venv/`、`__pycache__/` | Python 虚拟环境与缓存 |
| `hnrj-edu-uniapp/node_modules/` | npm 依赖 |
| `hnrj-edu-uniapp/unpackage/` | 打包产物（含 APK、安卓签名证书） |
| `湖南软件职业技术大学教务系统原生API接口文档.md` | 含真实个人信息的原始文档，仅存本地 |
| `*.db`、`.env`、`.DS_Store`、`Thumbs.db` | 数据库、环境配置、系统文件 |

两条规则：

1. 新增含账号、密码、密钥的配置时，先写进 `.gitignore` 再提交
2. 改代码时不要把真实学号、密码、姓名写回代码。当前仓库中的账号均为虚构模拟数据：
   - 新生：`202609011001` / `Demo@2026edu`（李明）
   - 老生：`202309011002` / `Demo@2026edu`（张伟）
   - 管理员：`admin` / `Admin@2026`

## 4. 原始文档与脱敏副本同步

- 本地原始文档 `湖南软件职业技术大学教务系统原生API接口文档.md` 保留真实数据，不入库
- 入库的脱敏副本为 `教务系统原生API接口文档.md`
- 以后重新抓包或补充接口信息时：
  1. 先更新本地原始文档（不入库）
  2. 再手动把新增内容同步到脱敏副本（入库），保持既有掩码风格：

| 数据类型 | 掩码风格 |
|----------|----------|
| 学号 | `2026****0000`（前 4 位 + `****` + `0000`） |
| 姓名 | `张某某` |
| 密码 | `password****` |
| 身份证号 | `431***********0000`（前 3 位 + 掩码 + `0000`） |
| 出生日期 | `2000****` |
| 班级 | `人工智能(专升本)****班` |
| JWT Token | 仅保留 `eyJhbGciOiJIUzUxMiJ9...`（header 部分） |

## 5. 提交前自查（约 30 秒）

```powershell
git status          # 查看将提交什么，确认没有不该提交的文件
```

若改动涉及数据，顺手全文搜索敏感串：

```powershell
git grep -n "202606140117\|@HSzero\|黄帅\|admin123"
```

有输出说明又把真实数据写回了代码，需要处理后再提交。

## 6. 分支策略

个人项目最简单的方式是**直接提交到 main**。改动较大时建议用功能分支：

```powershell
git checkout -b feature/xxx
# 修改、提交
git checkout main
git merge feature/xxx
git branch -d feature/xxx
```

## 7. 换电脑 / 重新获取代码

```powershell
git clone https://github.com/gitzero8/hnrj-edu-api.git
```

注意：

- 克隆后没有数据库文件，启动服务会自动生成模拟数据（seed 在代码中）
- 新电脑若遇到 SSL 报错，同样执行第 2 节的仓库级配置
- 原始接口文档不入库，如需请从原电脑手动拷贝
- 签名证书不在仓库中，发布 APK 前需从备份恢复

## 8. 出错回滚

```powershell
git log --oneline          # 查看提交历史
git revert <commit-id>     # 撤销某次提交（安全，保留历史）
```

警告：不要轻易使用 `git reset --hard` 与 `git push --force`，它们会覆盖历史记录。